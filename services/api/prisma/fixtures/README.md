# Fixtures de teste

Os arquivos de fixture do MVP ficam em **`apps/web/public/fixtures/`**:

| Arquivo | Uso |
|---------|-----|
| `sample-nfe.xml` | Upload NF-e (admin + Gate 2) |
| `metrics-sample.csv` | Import CSV métricas |

URLs no dev: `http://localhost:3000/fixtures/...`

Não duplicar aqui — uma única fonte evita drift.
